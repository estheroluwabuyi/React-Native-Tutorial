import DataFetching from "@/components/DataFetching";
import { View, Text } from "react-native";

const Home = () => {
  return (
    <View>
      <DataFetching />
    </View>
  );
};

export default Home;
